{
  "name": "graston-membership-directory",
  "version": "0.0.0",
  "dependencies": {
    "drizzle-orm": "^0.30.10",
    "hono": "^4.8.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230307.0",
    "typescript": "^4.9.5",
    "vitest": "^0.29.2",
    "wrangler": ">=3.1.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "schedule": "wrangler dev --test-scheduled",
    "db:generate": "wrangler d1 execute graston-membership-db --local --file=./schema.sql",
    "db:push": "wrangler d1 execute graston-membership-db --file=./schema.sql"
  }
}
